<svg viewBox="0 0 1200 800" xmlns="http://www.w3.org/2000/svg">
  <!-- Fondo con gradiente -->
  <defs>
    <linearGradient id="bgGradient" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#1C2A47;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#2A2E3E;stop-opacity:1" />
    </linearGradient>
    <linearGradient id="accentGradient" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#00BCD4;stop-opacity:0.2" />
      <stop offset="100%" style="stop-color:#00BCD4;stop-opacity:0" />
    </linearGradient>
    <filter id="blur">
      <feGaussianBlur in="SourceGraphic" stdDeviation="3" />
    </filter>
  </defs>

  <!-- Fondo principal -->
  <rect width="1200" height="800" fill="url(#bgGradient)" />

  <!-- Elementos decorativos de fondo -->
  <circle cx="150" cy="100" r="200" fill="url(#accentGradient)" />
  <circle cx="1050" cy="700" r="250" fill="url(#accentGradient)" />

  <!-- Frame principal (ventana de la app) -->
  <g id="app-frame">
    <!-- Barra de título -->
    <rect x="100" y="80" width="1000" height="60" rx="12" fill="#2A2E3E" stroke="#00BCD4" stroke-width="2" />
    <circle cx="130" cy="110" r="5" fill="#FF6B6B" />
    <circle cx="155" cy="110" r="5" fill="#FFC979" />
    <circle cx="180" cy="110" r="5" fill="#27AE60" />
    <text x="250" y="115" font-family="Inter, sans-serif" font-size="16" fill="#FFFFFF" font-weight="600">Anclora Adapt - Sistema de Generación Local</text>

    <!-- Panel lateral izquierdo (modos) -->
    <rect x="100" y="150" width="240" height="600" rx="8" fill="#1A1F3A" stroke="#00BCD4" stroke-width="1" opacity="0.8" />

    <!-- Título panel -->
    <text x="120" y="180" font-family="Inter, sans-serif" font-size="14" fill="#00BCD4" font-weight="700">MODOS</text>

    <!-- Items del menú -->
    <g id="menu-items">
      <!-- Modo Básico (activo) -->
      <rect x="110" y="200" width="220" height="45" rx="6" fill="#00BCD4" opacity="0.2" />
      <text x="130" y="228" font-family="Inter, sans-serif" font-size="13" fill="#00BCD4" font-weight="600">📝 Modo Básico</text>

      <!-- Modo Inteligente -->
      <text x="130" y="275" font-family="Inter, sans-serif" font-size="13" fill="#AAAAAA" font-weight="500">🧠 Modo Inteligente</text>

      <!-- Modo Campaña -->
      <text x="130" y="320" font-family="Inter, sans-serif" font-size="13" fill="#AAAAAA" font-weight="500">📊 Modo Campaña</text>

      <!-- Modo Reciclar -->
      <text x="130" y="365" font-family="Inter, sans-serif" font-size="13" fill="#AAAAAA" font-weight="500">♻️ Modo Reciclar</text>

      <!-- Chat Local -->
      <text x="130" y="410" font-family="Inter, sans-serif" font-size="13" fill="#AAAAAA" font-weight="500">💬 Chat Local</text>

      <!-- Modo Voz -->
      <text x="130" y="455" font-family="Inter, sans-serif" font-size="13" fill="#AAAAAA" font-weight="500">🎤 Modo Voz</text>

      <!-- Live Chat -->
      <text x="130" y="500" font-family="Inter, sans-serif" font-size="13" fill="#AAAAAA" font-weight="500">⚡ Live Chat</text>

      <!-- Modo Imagen -->
      <text x="130" y="545" font-family="Inter, sans-serif" font-size="13" fill="#AAAAAA" font-weight="500">🖼️ Modo Imagen</text>
    </g>

    <!-- Panel principal derecho -->
    <rect x="360" y="150" width="720" height="600" rx="8" fill="#0F1723" stroke="#00BCD4" stroke-width="1" opacity="0.9" />

    <!-- Header del contenido -->
    <rect x="360" y="150" width="720" height="50" rx="8" fill="#1A1F3A" />
    <text x="380" y="182" font-family="Inter, sans-serif" font-size="14" fill="#00BCD4" font-weight="600">Generador de Contenido Multimodal</text>

    <!-- Input de prompt -->
    <rect x="380" y="220" width="680" height="80" rx="6" fill="#1A1F3A" stroke="#00BCD4" stroke-width="1" opacity="0.5" />
    <text x="400" y="245" font-family="Inter, sans-serif" font-size="12" fill="#AAAAAA" font-weight="400">Ingresa tu prompt aquí...</text>
    <text x="400" y="265" font-family="Inter, sans-serif" font-size="11" fill="#666666" font-weight="400">Ejemplo: "Crea un copy para campaña de marketing de productos eco-friendly"</text>
    <text x="400" y="285" font-family="Inter, sans-serif" font-size="11" fill="#666666" font-weight="400">Procesamiento 100% local • Sin límite de tokens • Privacidad total</text>

    <!-- Botones de acción -->
    <rect x="380" y="315" width="150" height="40" rx="6" fill="#00BCD4" />
    <text x="405" y="342" font-family="Inter, sans-serif" font-size="13" fill="#000000" font-weight="600">Generar</text>

    <rect x="545" y="315" width="115" height="40" rx="6" fill="none" stroke="#00BCD4" stroke-width="2" />
    <text x="560" y="342" font-family="Inter, sans-serif" font-size="13" fill="#00BCD4" font-weight="600">Limpiar</text>

    <!-- Área de output (resultados) -->
    <text x="380" y="375" font-family="Inter, sans-serif" font-size="12" fill="#00BCD4" font-weight="600">RESULTADOS GENERADOS</text>

    <!-- Card de resultado -->
    <rect x="380" y="395" width="320" height="140" rx="6" fill="#1A1F3A" stroke="#00BCD4" stroke-width="1" opacity="0.5" />
    <text x="400" y="420" font-family="Inter, sans-serif" font-size="11" fill="#FFC979" font-weight="600">📱 Instagram</text>
    <text x="400" y="445" font-family="Inter, sans-serif" font-size="10" fill="#E8E8E8" font-weight="400">Discover the future of sustainable living with our eco-friendly collection. Every purchase supports the planet. 🌍✨ #SustainableLiving #EcoFriendly</text>
    <rect x="400" y="510" width="100" height="25" rx="4" fill="#27AE60" opacity="0.3" />
    <text x="415" y="526" font-family="Inter, sans-serif" font-size="10" fill="#27AE60" font-weight="600">Copiar</text>

    <!-- Card de resultado 2 -->
    <rect x="720" y="395" width="320" height="140" rx="6" fill="#1A1F3A" stroke="#00BCD4" stroke-width="1" opacity="0.5" />
    <text x="740" y="420" font-family="Inter, sans-serif" font-size="11" fill="#FFC979" font-weight="600">📧 Email Marketing</text>
    <text x="740" y="445" font-family="Inter, sans-serif" font-size="10" fill="#E8E8E8" font-weight="400">Subject: Transform Your Lifestyle Today - 50% Off Eco Products. Conscious choices. Real impact. Shop now →</text>
    <rect x="740" y="510" width="100" height="25" rx="4" fill="#27AE60" opacity="0.3" />
    <text x="755" y="526" font-family="Inter, sans-serif" font-size="10" fill="#27AE60" font-weight="600">Copiar</text>

    <!-- Status bar inferior -->
    <rect x="360" y="730" width="720" height="20" rx="4" fill="#1A1F3A" opacity="0.5" />
    <text x="380" y="742" font-family="Inter, sans-serif" font-size="10" fill="#00BCD4" font-weight="400">✅ Procesamiento completado en 2.3s • 2 plataformas generadas • 100% privado</text>
  </g>

  <!-- Etiqueta de feature -->
  <g id="badge">
    <rect x="850" y="30" width="280" height="35" rx="20" fill="#00BCD4" opacity="0.15" stroke="#00BCD4" stroke-width="1" />
    <text x="870" y="52" font-family="Inter, sans-serif" font-size="12" fill="#00BCD4" font-weight="600">✨ Interfaz Intuitiva • Local-First • Sin Dependencias</text>
  </g>
</svg>